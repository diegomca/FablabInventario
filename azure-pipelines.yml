# Node.js with React
# Build a Node.js project that uses React.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    npm run build

  displayName: 'npm install and build'

- script: |
    git config user.email "diego.monsalves@alumnos.uv.cl"  
    git config user.name "Diego Monsalves"
    git checkout master
    git add --all
    git push https://74df474597a84a6fb47b22eb9f47ff21efadd68f@github.com/diegomca//FablabInventario.git --force
  displayName: 'Github'